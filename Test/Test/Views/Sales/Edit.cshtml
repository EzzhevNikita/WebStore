@model Test.Models.SellerPanelViewModel
@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Online Store</title>
</head>
<body>
    <form action="" method="post">
        <div>
            <h3>id</h3>
            <div>
                <label for="id">id:</label>
                @Html.DisplayFor(md => md.id)
            </div>
            <h3>Name</h3>
            <div>
                <label for="Name">Name:</label>
                @Html.EditorFor(md => md.Name)
            </div>
            <h3>Description</h3>
            <div>
                <label for="Description">Description:</label>
                @Html.EditorFor(md => md.Description)
            </div>
            <h3>Price</h3>
            <div>
                <label for="Price">Price:</label>
                @Html.EditorFor(md => md.Price)
            </div>
            <h3>Category</h3>
            <div>
                <label for="Category">Category:</label>
                @Html.EditorFor(md => md.Category)
            </div>
            <div>
                <input type="submit" name="Edit" />
            </div>
        </div>
    </form>
</body>
</html>